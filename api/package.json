{
  "name": "@pophq/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test:unit": "vitest run --project unit",
    "test:integration": "vitest run --project integration",
    "db:create": "tsx scripts/create-table.ts",
    "db:seed": "tsx scripts/seed.ts",
    "db:reset": "tsx scripts/create-table.ts --reset && tsx scripts/seed.ts",
    "admin": "tsx scripts/admin.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1136.0",
    "@aws-sdk/client-ssm": "^3.1136.0",
    "@aws-sdk/lib-dynamodb": "^3.1136.0",
    "@hono/node-server": "^2.1.1",
    "hono": "^4.13.8",
    "jose": "^6.2.12",
    "ulid": "^3.0.2",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@aws-sdk/client-cloudformation": "^3.1136.0"
  }
}
